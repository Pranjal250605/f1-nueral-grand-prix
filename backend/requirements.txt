# Neural Grand Prix — Backend Dependencies
# Math / numerics
numpy>=1.26.0

# API server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# HTTP client (GitHub API calls)
httpx>=0.27.0

# TTS — free Google Translate speech synthesis (no API key needed)
gTTS>=2.5.0

# AI commentary — Claude streaming (optional; app degrades gracefully without it)
anthropic>=0.31.0
